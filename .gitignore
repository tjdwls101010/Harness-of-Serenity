# --- Secrets: NEVER commit ---
.env
.env.*
!.env.example

# --- Python venv & caches ---
**/.venv/
**/__pycache__/
*.pyc
*.pyo
.pytest_cache/

# --- OS / editor cruft ---
.DS_Store
*.swp

# --- Scratch / local validation artifacts ---
.tmp/
scripts/**/.Seongjin/

# --- Dev-time meta-tooling, not part of the harness deliverable ---
.claude/skills/skill-creator/

# Build-time scaffolding kept local; not published with the GitHub project.
references/

# Runtime state cache (not a deliverable)
data/state_store.db/
.serenity/
/sessions/

# Finalized decision lineage is a researcher's own output, not part of the published harness.
records/

# --- Runtime loop/research artifacts & scratch (not deliverables) ---
.omo/
scratchpad_macro.json

# Codex run registry is local runtime state. The bundled `.codex -> .claude` symlink is tracked.
.codex-runs/
