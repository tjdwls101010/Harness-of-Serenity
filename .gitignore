# --- Secrets: NEVER commit ---
.env
.env.*
!.env.example

# --- Python venv & caches ---
**/.venv/
**/__pycache__/
*.pyc
*.pyo
.pytest_cache/

# --- OS / editor cruft ---
.DS_Store
*.swp

# --- Scratch / local validation artifacts ---
.tmp/
scripts/**/.Seongjin/

# --- Dev-time meta-tooling, not part of the harness deliverable ---
.claude/skills/skill-creator/

# Build-time scaffolding kept local; not published with the GitHub project.
references/

# Runtime state cache (not a deliverable)
data/state_store.db/

# --- Runtime loop/research artifacts & scratch (not deliverables) ---
.omo/
scratchpad_macro.json

# Codex run registry + local .codex convenience symlink (the committed .codex/ layer was retired
# 2026-08-11; these are local tooling state, not a harness component)
.codex-runs/
/.codex
