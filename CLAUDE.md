`./.claude/skills/Serenity`는 내가 `X(twitter)`의 세계 최고로 유명하고 높은 수익률을 내는 주식 애널리스트 `serenity`의 그동안 thesis를 통해 만든 나만의 스킬이야.
`./.claude/skills/Serenity/References/analysis_Serenity.db`는 실제 `serenity`의 thesis들을 모아둔 db이고.

스킬은 단순히 마크다운 파일이 아니야.
문서(SKILL.md + References)와 코드(pipeline + module)의 합인 폴더지.
llm이 고유로 가지고 있는 지능과 창의성은 유지하면서도 역량을 추가하는게 목표인거지.
그렇게 하려면 목적과 방향성은 명확하게 정의하되, 그를 향해 나아가는 방법은 hard하게 규정해서는 안되.
hard하게 규정한다면 상황에 따라 유연하게 판단하지 못할테니까.
스킬에선 규칙을 나열하기 보다는 그 규칙이 왜 좋으며, 왜 필요한지를 llm이 스스로 이해하고 체화할 수 있도록 원칙 중심으로 설계를 해야해.
또한 이미 llm이 아는걸 길게 늘여쓰면, 막상 중요한 내용은 묻혀버리고 컨텍스트를 낭비해 성능이 저하될 우려가 크지.
그래서 아주 밀도 높은 원칙 중심으로 작성을 하라는거지.
코드는 클로드가 활용할 수 있도록, 파라미터를 받는 cli형태로 만들어야지.
그냥 아무런 파라미터도 받지 않도록 만들어진 코드는 클로드가 사용할 tool이라고 말하기 어려울테니까.

현재 `serenity` 스킬을 작성할 때, 내가 가장 중요하게 생각했던건 문서와 코드의 명확한 역할 분담이야.
앞서 말했듯 문서를 통해 `serenity`의 형식지와 암묵지, 즉 원칙을 체화해 llm의 지능과 결합하도록 했고.
코드는 결정론적인 신뢰도 높은 데이터를 로드하도록 했어.
예컨데 현재 주가나 어닝 등의 데이터를 websearch로 검색해서 사용하라고 하면, 자칫 부정확한 데이터를 로드할 가능성도 크고 검색 결과에 노이즈가 포함될 가능성이 크지.
그래서 되도록이면 기업의 벨류체인과 같이 narrative적인 데이터는 websearch로 로드를 하더라도, 정확성이 요구되는 숫자는 코드로 로드를 하고 싶었거든.
그리고 코드는 pipeline과 module로 이중화했어.
`serenity`는 로드해야 할 데이터의 종류는 많은데, 이걸 각 module코드를 하나하나 실행해서 얻으려면 실행해야 하는걸 실행하지 않고 넘어간다든지 부족한 점이 발생할 수 있다고 생각해서 module코드를 종합해서 코덱스 입장에서 보기에 깔끔한 형태의 하나의 json으로 받도록 하는 pipeline코드도 만들었어.
근데 지금 pipeline코드의 아웃풋 스키마가 지금 다소 불필요한 정보가 많이 포함된 느낌이야.
그리고 아까 말했듯 문서와 코드의 역할이 다른데, 문서를 체화한 코덱스가 판단해야 하는 비결정론적인 부분까지도 코드에서 flag/signal/score 등으로 계산이 되어 잘못된 방향으로 유도를 할까 두려워.
flag/signal/score를 정말로 criteria를 잘 만들면 괜찮겠지만, 100개 중 하나라도 뭔가 잘못되었다면, 전체 시스템에 악영향을 줄 여지가 크다고 생각하거든.
그래서 코드를 통해 판단의 근거만을 가져오고, 판단은 코덱스가 하도록 하는 방향으로 전환을 하는게 좋지 않을까 생각이 들어.

이미 내가 만든 `serenity`스킬은 하나의 스킬에 모든 기능을 다 채웠는데.
그보단 AGENTS.md, 스킬들, hook들 등으로 나눠서 ai-harness를 다시 만드는게 좋지 않을까라는 생각이 들어.
`./.tmp/omo`는 내가 아는 최고 실력의 개발자가 만든 ai-harness인데, 이걸 만든 개발자의 ai-harness 제작 역량을 간접적으로나마 참고할 수 있지 않을까 싶어.
`./.tmp/omo`은 코덱스용 harness이기 때문에, 클로드코드에 맞는 harness의 양식과 같진 않지만 참고를 할만할 거 같아.

서브에이전트와 dynamic-workflow를 아주 적극적으로 사용해도 되.
토큰과 비용 걱정하지 말고 오직 퀄리티만을 생각하며 진행하면 좋겠어.
난 개발에 대한 이해가 너보다 현저히 부족해.
반면 너는 세계 최고 수준의 개발 역량을 가지고 있지.
지능과 학습 능력도 나보다 훨씬 뛰어나고.
그래서 내 목적을 이해하고, 그 목적을 달성하는 시스템을 만들어주길 바라는 거거든.

깃허브는 `[tjdwls101010/Harness-of-Serenity.git](https://github.com/tjdwls101010/Harness-of-Serenity.git)`에 연결해서 관리해줘.
한개의 스킬을 만들라는게 아니라, `CLAUDE.md`, hook과 skill들 여러개로 구성된 harness를 만들어 달라는 뜻인거 이해하지?
지금 하나의 스킬에 모든 내용을 다 넣는게 의도대로 작동하는데 한계가 있다고 생각이 들어서, 지금 이 작업을 하는거니까.