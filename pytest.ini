[pytest]
testpaths = tests
addopts = -m "not live"
markers =
    live: probes a real provider interface over the network. Excluded by default and never run in CI; run with `pytest -m live` after a pinned library or endpoint moves. These assert the object surface and types the adapters parse, which no recorded payload can pin because edgartools and yfinance return Python objects rather than JSON.
